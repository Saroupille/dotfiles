\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{custom-general}[2016/12/12 General custom macros]

%better support for output of accentued characters
\RequirePackage[T1]{fontenc}

%allows the user to enter accentued characters
\RequirePackage[utf8]{inputenc}

% allows new unicode characters
\RequirePackage{newunicodechar}

% better font
\RequirePackage{lmodern}

% Better support for graphicx stuff
\RequirePackage{graphicx}

% Add URL/URI inside a doccument
\RequirePackage{hyperref}

% allow syntax coloring for code snippets
\RequirePackage{minted}
\endinput